org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Allow building on Windows when project path contains non-ASCII characters
android.overridePathCheck=true

# Redirect Flutter/Gradle build directory to an ASCII-only path to avoid OneDrive/non-ASCII issues
# Update this path if desired.
flutter.buildDir=C:\\Users\\Public\\flutter_build\\schelper_app
